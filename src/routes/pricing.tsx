import { createFileRoute, Link } from "@tanstack/react-router";
import { Check } from "lucide-react";
import { MarketingFooter, MarketingHeader, PageIntro } from "@/components/lax/marketing";
import { Button } from "@/components/ui/button";

export const Route = createFileRoute("/pricing")({ head: () => ({ meta: [{ title: "Pricing — LAX" }, { name: "description", content: "Simple plans for converting screenshots and Figma designs into code." }, { property: "og:title", content: "LAX Pricing" }, { property: "og:description", content: "Simple plans for design-to-code workflows." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: PricingPage });
const plans = [{ name: "Free", price: "$0", note: "For testing the workflow", features: ["10 conversions / month", "HTML & CSS export", "Community support"] }, { name: "Pro", price: "$24", note: "For designers and developers", features: ["150 conversions / month", "Figma import", "Asset bundles", "Priority processing"], featured: true }, { name: "Studio", price: "$79", note: "For product teams", features: ["Unlimited projects", "500 conversions / month", "Shared workspace", "Priority support"] }];
function PricingPage(){return <div className="page-glow min-h-screen"><MarketingHeader/><main className="mx-auto max-w-7xl px-6 py-20"><PageIntro eyebrow="Pricing" title="Clean plans. No code tax." description="Start with a real workflow for free, then scale when LAX becomes part of how your team ships."/><div className="mt-14 grid gap-5 lg:grid-cols-3">{plans.map(p=><article key={p.name} className={`rounded-xl border p-7 ${p.featured?"border-primary bg-primary/10 shadow-[var(--shadow-glow)]":"border-glass-border bg-glass"}`}><p className="font-display text-xl font-semibold">{p.name}</p><p className="mt-6 font-display text-5xl font-semibold">{p.price}<span className="text-sm font-normal text-muted-foreground"> / month</span></p><p className="mt-2 text-sm text-muted-foreground">{p.note}</p><Button asChild variant={p.featured?"hero":"glass"} className="mt-7 w-full"><Link to="/signup">Choose {p.name}</Link></Button><ul className="mt-7 space-y-3">{p.features.map(f=><li key={f} className="flex gap-2 text-sm"><Check className="size-4 text-primary"/>{f}</li>)}</ul></article>)}</div></main><MarketingFooter/></div>}