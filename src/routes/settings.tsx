import { createFileRoute } from "@tanstack/react-router";
import { AppHeader, AppShell } from "@/components/lax/app-shell";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Switch } from "@/components/ui/switch";
export const Route=createFileRoute("/settings")({head:()=>({meta:[{title:"Settings — LAX"},{name:"description",content:"Manage your LAX workspace settings."},{property:"og:title",content:"Settings — LAX"},{property:"og:description",content:"Manage profile and conversion preferences."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:SettingsPage});
function SettingsPage(){return <AppShell><AppHeader eyebrow="Preferences" title="Settings"/><div className="mt-8 max-w-3xl space-y-5"><section className="glass-panel rounded-xl p-6"><h2 className="font-display text-xl font-semibold">Profile</h2><div className="mt-5 grid gap-4 sm:grid-cols-2"><label className="text-sm">Name<Input className="mt-2 bg-background/40" defaultValue="Alex Kim"/></label><label className="text-sm">Email<Input className="mt-2 bg-background/40" defaultValue="alex@studio.dev"/></label></div><Button variant="hero" className="mt-5">Save changes</Button></section><section className="glass-panel rounded-xl p-6"><h2 className="font-display text-xl font-semibold">Conversion defaults</h2>{[["Generate responsive rules","Infer desktop, tablet, and mobile layouts."],["Include design tokens","Extract colors, type, spacing, and radii."],["Optimize assets","Prepare web-friendly image exports."]].map(([a,b],i)=><div key={a} className="flex items-center justify-between gap-5 border-b border-border py-5 last:border-0"><div><p className="font-medium">{a}</p><p className="mt-1 text-sm text-muted-foreground">{b}</p></div><Switch defaultChecked={i<2} aria-label={a}/></div>)}</section></div></AppShell>}